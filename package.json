{
  "name": "weid-immobilien-root",
  "private": true,
  "scripts": {
    "dev": "cd website && bun run dev",
    "v1:dev": "cd website/v1 && bun run dev",
    "build": "cd website && bun run build",
    "v1:build": "cd website/v1 && bun run build",
    "start": "cd website && bun run start",
    "lint": "cd website && bun run lint"
  }
}
